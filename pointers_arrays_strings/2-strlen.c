#include "main.c"
#include <stdio.h>

/**
 * _strlen - returns to string.
 * @str: the string of length.
 *
 * Return: the lenghth of @str.
 */
int _strlen(char *str)
{
	size_t lenght = 0;

	while (*str++)
		length++;

	return (length);
}
